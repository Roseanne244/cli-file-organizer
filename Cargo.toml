[package]
name = "cli-file-organizer"
version = "0.1.0"
edition = "2021"
description = "Automatically organize files into folders by extension"
authors = ["Roseanne Park"]
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
colored = "2"
walkdir = "2"
